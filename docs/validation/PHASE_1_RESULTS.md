# 第一阶段执行与实测记录

日期：2026-09-17 · 状态：新 A 方向横屏已实现，进入用户测试移交

## 当前摘要（优先于下方历史流水）

- 最新版本为 Windows 0.1.1，构建指纹 `cd32936493138d18`；完整范围、证据、长测状态与缺口见 [第一阶段移交](PHASE_1_HANDOFF.md)。
- 已重建为分层 2.5D 云谷、古典蒸汽列车、独立 30 分钟昼夜与程序化区段；已完成 48 项测试、类型/lint、生产构建与 Windows 新包。
- 新版 18 格横屏昼夜矩阵、五分钟实际运行视频、最终构建平台探针已保存。用户审美验收与 Mac/低配实测仍待反馈。
- 以下保留旧原型的历史过程；其中“当前”“待实现”和旧性能数字只适用于当时的版本，不作为新版结论。

以下为历史记录，保留各次结果及当时缺口，不作为当前完成清单。

## 环境

- Windows 开发机：i7-12700KF、RTX 3080、32 GB RAM；显卡驱动 32.0.15.9186，系统读取的当前分辨率 3840×2160。
- Node 24.9.0、pnpm 11.18.0、Rust 1.98.1（本阶段安装，未修改永久 PATH）。
- 已发现 Visual Studio 2022 Build Tools；Windows release 可执行文件编译成功。
- Mac 2019／2014、Windows 集显、Apple Silicon 尚未实测。

## 自动化证据

2026-09-15：18 项 Vitest 测试通过，TypeScript 严格检查与 ESLint 通过，Vite 生产构建通过。具体测试源：`tests/core.test.ts`。后续代码变化必须再次执行检查，以上不能自动视为最终版本结果。

## 浏览器检查

使用 Codex 内置浏览器，原型 `http://127.0.0.1:5173/`。

- 1440×900 横屏设置面板、540×960 竖屏实际渲染已查看。
- 自动切换竖版、手动选竖版、设置展开／关闭已检查。
- 暖晨色调、20 FPS 省电档、Relax 文案、换 seed、键盘滑杆调整与暂停已观察到真实状态变化。
- 竖版早期列车裁切已修复，最新版截图中三节列车完整。
- 省电档诊断示例约 50 ms/帧、CPU 提交 0.4–0.6 ms，70 draw calls、5 geometries、0 textures；这是瞬时本机示例，不代表 GPU 耗时、功耗或低配结论。
- 读取浏览器错误日志时未见错误；尚需最终版本回归。

## 原生与长期测试

- Windows release 原生程序曾成功启动；没有将进程存在当作完整 UI 验收。
- 新增原生 `--self-test` 与 JSONL 本地日志，准备执行 50 次切换及 2 小时运行。
- 控制／播放双窗口探针、锁屏／睡眠桥接、拔屏恢复、完整交互验收尚未完成。
- 安装包、macOS CI 实际运行、Mac 与低配功耗验证尚未完成。

## 当前缺口

1. 继续改善云层、远近关系，最终截图对照和用户视觉反馈未完成。
2. 完成原生平台探针与系统生命周期处理。
3. 完成长期运行、50 次切换及总关联进程内存记录。
4. 补齐 21:9、1:1、4:3、多个种子与画质的交互／视觉矩阵。
5. 完成测试说明、CI、运行证据和任务逐项审计。
6. 外部设备配置及 Mac 实机反馈待获得；不能宣布 M0 全部通过。

## 18:59 增量检查

- 修复 Esc：优先于输入控件快捷键过滤处理，关闭面板并请求退出全屏；桌面调用明确设为 false，避免重复 Esc 反而进入全屏。
- 新增 3 项平台适配测试，覆盖桌面重复退出、网页非全屏空操作和平台拒绝错误传播。21 项测试、类型检查、lint、网页生产构建全部通过。
- 内置浏览器 localhost:5173、1280×720：页面正常渲染，Esc 后设置面板消失，错误／警告日志为空。点击全屏按钮后 document.fullscreenElement 仍为 null，因此此浏览器环境未证明全屏进入／退出链路；原生实际全屏回归仍待完成。
- Vite 报告场景分块约 512 kB 的体积警告；构建成功，但不将此警告解释为运行内存或 GPU 负载。
- 原生 PID 81624 已确认仍存活；日志最近约 8 分钟，尚无两小时完成证据。18:58 进程树采样包含 7 个进程，工作集总和 382,341,120 字节，私有内存 315,486,208 字节；前者约 365 MiB，超过初始 200–300 MB 目标，需要继续观察与优化，不能宣布低功耗达标。
- 本次网页修改尚未重新编入正在运行的原生长测程序，长测结果只适用于它启动时的版本。
- 随后修复首次 render/resize 抛错时的活动实例清理：已释放实例不再留在帧循环中。回归覆盖释放一次、错误报告及重新加载成功；测试增至 22 项，全部通过。

## 19:03 双窗口增量

- 独立控制窗口增加显示器枚举，选屏命令由播放窗口执行；新增音频测试请求与结果消息，声音仍由播放窗口创建。真实跨窗口解锁结果待原生运行验证。
- 播放状态快照使用 memo，诊断计时刷新不再重复发送相同状态；补齐控制页事件订阅失败处理与音频结果订阅清理。
- `pnpm typecheck`、`pnpm lint`、22 项测试及网页生产构建通过；`tauri build --debug --no-bundle` 成功，产物位于 `apps/player/src-tauri/target/debug/lumawindow.exe`。本次构建与运行中的 release 长测程序分开，未重启长测。
- 网页重新加载后正常显示设置与场景；这不是原生双窗口运行证据。安装包和 Mac 构建仍未验收。
- 已提供 [朋友设备测试说明](./DEVICE_TEST_GUIDE.md)，涵盖配置记录、音频、双窗口、系统事件与功耗反馈模板；没有替用户联系或发送给朋友。

## 19:05 Windows 原生平台探针

当前调试构建通过 `--platform-test` 运行，原始证据见 [windows-platform-probe.jsonl](./windows-platform-probe.jsonl)。日志以 `platform-complete` 结束，测试进程随后停止；release 长测未被重启。

| 检查 | 结果与证据边界 |
| --- | --- |
| 显示器枚举 | 两块屏幕：3840×2160／缩放 2，2160×3840／缩放 1.75 |
| 全屏 API | 实际原生窗口进入／退出后分别读取 isFullscreen，状态正确；不等同键盘 Esc 人工检查 |
| 控制窗口创建与握手 | 创建独立 WebView，收到初始状态，控制页报告 canvas 数量为 0 |
| 暂停／恢复消息往返 | 控制页回传状态与引擎 paused 一致；暂停半秒活动时间保持，恢复后继续递增 |
| 关闭控制页 | 原生窗口句柄消失，播放活动时间继续推进 |
| 音频／选屏定位／锁屏／睡眠／拔屏 | 本次未覆盖，保持待测 |

探针是应用内 API 与事件链路验证，不冒充用户点击、原生视觉检查或完整系统事件验收。调试进程约 19:04–19:05 与 release 长测同时存在，这段资源数据含额外测试负载，不能用于干净的办公功耗对比。22 项单元测试、lint、类型检查与更新后的 Windows 调试构建通过。

## 19:08 测试音生命周期

- 测试音支持立即停止；重复请求先结束上一请求。音频解锁有两秒超时，取消后迟到的解锁不会启动声音；播放页隐藏、离开或卸载时停止测试音。
- 控制窗口共享音频活动状态，显示“停止测试音”，通过消息请求播放窗口停止；自身不创建 AudioContext。
- 新增五项音频资源生命周期测试，总计 27 项通过；类型检查、lint、网页构建通过。
- 内置浏览器 1280×720 实际点击“测试声音”→观察“停止测试音”→立即点击停止→恢复“测试声音”。错误／警告日志为空。验证了按钮与真实音频模块状态链路；没有把此结果写成对听感、系统输出设备或 Mac 解锁的验证。
- 音频更新后的原生构建与跨窗口音频回归仍待完成；此前平台探针通过不自动覆盖这些新改动。

## 19:12 画面与提交量优化

- 修正云体非均匀缩放的法线，调整云团比例和间隙；云层雾距现在响应雾感参数。增加静态远山轮廓，移除未使用的桥柱实例，复用每帧颜色对象，投影矩阵只在 FOV 或尺寸变化时更新。
- 云团以保守包围球做视野裁剪；所有云团仍持续更新生成代次，画面外云团不提交 GPU，不更换随机种子或削减可见世界。
- 默认 seed 8472 的竖屏 540×960 样本约 281,320 triangles，旧实现默认约 474,680；减少约 41%，但尚无对应功耗对照。新增远山后 draw calls 为 71、geometries 为 6、textures 为 0。
- 21:9 样本约 439,720 triangles，1:1 样本约 312,120；收益随视野不同，不能将竖屏比例概括成所有模式的提升。
- 实际截图检查了 1280×720、540×960、1680×720、720×720，主体完整，无页面错误日志；21:9 内部像素 1527×654，未突破平衡档约 1M 预算。
- 27 项既有测试、类型检查、lint 与网页构建通过；这些逻辑测试不验证最终美术或云团裁剪的长期边缘行为。多种子动态矩阵、视觉反馈和更新后长测仍需完成。现有 release 长测不包含本次场景修改。

## 19:16 帧调度暂停与原生回归

- 引擎在暂停、页面隐藏、等待实例创建时取消 RAF；可见且未暂停、实例就绪时才重新调度。重复 start／resume 不创建额外循环，dispose 清理可见性监听。
- 新增三项调度回归：无场景不唤醒、暂停冻结／恢复不跳时、隐藏后保持手动暂停、销毁后不复活、异步创建期间不唤醒。暂停 resize 只重绘静止画面，不推进时钟或重开循环。总计 30 项测试通过。
- Windows 调试构建与原生 `--platform-test` 回归通过，最新原始证据已追加到 windows-platform-probe.jsonl。音频活动字段也成功同步为 false，但这不等于音频播放测试。探针进程已停止。
- 浏览器暂停后调整到 960×720，静止画面仍可见。随后补齐场景 resize 时 FOV 与云团裁剪刷新，保持 seed 和活动时间；该最后补丁类型检查、lint 通过，尚未重新编入原生探针构建。
- 旧 release 长测继续运行。约 19:15–19:16 有额外调试实例和构建负载，需从干净功耗对照中排除。

## 19:20 Windows 系统事件桥接

- 为主播放窗口注册原生窗口消息子类与 WTS 会话通知，接收锁屏／解锁、睡眠／恢复、最小化／恢复及显示器变更；销毁窗口时注销通知并释放回调持有的句柄。macOS 对应原生桥接尚未实现，状态查询明确返回不支持，不能按 Windows 结果推断。
- 引擎分别保存 session、sleep、minimized 暂停原因。解除一个原因不会清掉其他原因，也不修改用户的手动暂停；暂停事件停止测试音，恢复事件不主动发声。新增重叠原因测试后，31 项测试通过。
- Windows 原生 `lifecycle_status` 返回 bridgeInstalled=true、sessionNotifications=true、error=null。实际最小化／恢复通过窗口消息到引擎的链路验证，最小化期间活动时间冻结，恢复后递增；平台日志再次以 platform-complete 结束。
- 锁屏与睡眠仅完成实现与状态逻辑测试，尚未实机触发；没有用模拟消息冒充系统事件实测。显示器变化仅刷新列表，拔屏后的窗口可见区域恢复仍待实现。
- Rust 检查、TypeScript、lint、网页构建和 Windows 调试构建通过。测试实例已停止，原始日志已更新；约 19:20 额外测试负载不作为干净功耗对照。
- 实现参考：[Microsoft 会话状态通知](https://learn.microsoft.com/en-us/windows/win32/termserv/wm-wtssession-change)、[窗口子类链说明](https://learn.microsoft.com/en-us/windows/win32/controls/subclassing-overview)。

## 19:25 显示器定位与越界恢复

- 新增工作区坐标恢复算法，支持负坐标竖屏、标题栏越界、过大窗口和空显示器列表；四项测试加入后共 35 项通过。
- 显示器变化事件触发窗口可见性检查，失去可操作区域时移回最近可用工作区。选屏按工作区尺寸扣除边框，不覆盖任务栏；恢复并发请求合并。
- 首次原生测试暴露混合 DPI 缺陷：先设置尺寸再跨屏移动会被 Windows 二次缩放。旧日志 19:23 的 platform-complete 只检查了标题栏可见，不能证明整个窗口适配。已修正为先移动，再测量目标屏幕上的边框并设置尺寸，同时加强探针的完整边界检查。
- 19:25 原生日志确认：移到 175% 竖屏后，外框 2160×3756，与工作区一致；移到屏幕外后恢复为主屏内的 2160×2016，完整边界检查通过。测试最终以 platform-complete 结束，实例已停止。
- 此次测试通过应用 API 移动真实窗口，验证了定位／恢复算法与混合 DPI 行为；没有真实拔掉显示器，不将其记作拔线事件时序验收。约 19:23–19:25 的额外调试实例与构建负载需排除出干净功耗对照。
- 类型检查、lint、网页与 Windows 调试构建通过，原始日志已更新。

## 19:29 桌面打包与版本追踪

- 实际执行根命令 `pnpm desktop:build`，在独立 CARGO_TARGET_DIR 下成功生成 Windows NSIS 安装包，首个包为 1,646,034 字节。仅生成本地测试包，没有安装、签名、发布或上传；安装／卸载流程不因包存在而算通过。
- 独立输出路径：`.local/release-build/release/bundle/nsis/LumaWindow_0.1.0_x64-setup.exe`。配置中的 macOS dmg 目标未妨碍 Windows 打包，但本次没有生成 Mac 产物。
- 随后加入源码输入指纹，未来自测与平台探针日志带 buildId；旧日志没有该字段，不能回填成新版本证据。`apps/player/vite.config.ts` 与指纹脚本也纳入 TypeScript 检查。
- 修复 lint 错误扫描独立构建目录的问题：仅排除 .local／.tools 编译与工具产物，继续检查源码。35 项测试、类型检查、lint、网页构建通过。
- 包含指纹的第二次 release 打包已完成。源码 buildId 为 `7387a7823aa90bcf`，安装包 1,645,754 字节，SHA-256 为 `D32E244D36EB404CDE9CC63212ECE27A2A94DCB419805F6D45BF5B6CD31F994A`。
- 19:31 使用该 release 可执行文件运行平台探针，日志中的 buildId 与源码一致，全屏、最小化／恢复、竖屏工作区适配、越界恢复、双窗口状态往返和关闭控制页后继续播放全部通过。探针结束后测试实例已停止；安装／卸载和正式分发仍未验收。
- 旧 release 长测进程仍存活；前 39 条进程树采样工作集从约 340.8 MiB 到 378.8 MiB，峰值 475.4 MiB；私有内存从 277.7 MiB 到 316.7 MiB。测试混有构建与短期探针负载，需结合完整趋势分析，不能提前判定内存达标或泄漏。

## 19:35 图形错误恢复

- 重试使用独立的 reloadVersion，不再改变种子或强行取消用户暂停。渲染异常改为独立故障暂停原因，成功重建后解除；失败加载不再启动自测流程。
- 上下文丢失时暂停引擎并停止测试音；收到恢复事件后自动重载当前场景。着色程序编译失败通过错误回调进入可见错误处理，不仅输出控制台日志。
- 新增故障后保留用户暂停的回归测试，共 36 项通过；类型检查、lint、网页和 Windows 调试构建通过。
- buildId `fe8e6ddcf413e6f3` 的原生探针通过 WEBGL_lose_context 接口触发真实上下文丢失／恢复事件。日志确认 suspended 后换成新实例，恢复活动时间和绘制（6 geometries、0 textures、71 draw calls），最终 platform-complete。此结果不等同于实际显卡驱动崩溃实测。
- 测试实例已停止，原始日志已更新。此前生成的安装包仍对应 `7387a7823aa90bcf`，尚未包含本节修复；最终交付前需刷新安装包。

## 控制窗口屏幕列表刷新补齐

- 控制窗口现在监听 displays-changed，Windows 原生桥同时向播放窗口与控制窗口通知显示器变化；控制窗口重新获得焦点时也刷新列表。
- 先注册事件再首次枚举；用请求序号避免旧枚举结果覆盖新结果，卸载时清理监听并忽略未完成请求。
- TypeScript、lint、36 项现有测试、cargo check、网页生产构建通过。现有测试不覆盖此 React 事件链路，真实拔屏回归仍待验证。
- 重新独立查询确认旧版长测 PID 81624 仍存活，日志达到 wallSeconds 3360.76；不因一次合并输出未显示进程就判定停止。该旧版本仍不构成当前代码两小时验收证据。

## 静态视觉矩阵采集完成

18 张截图覆盖 9:16、16:9、21:9，每个比例两个构图、每构图三个种子。记录与缺陷见 [视觉矩阵](./VISUAL_MATRIX.md)。超宽屏新增六张已实际查看，控制台无错误／警告。静态主体完整性与比例适配有证据；云层造型、底色空洞、五分钟连续动态检查仍未通过视觉验收。

## 云底连续层调整

- 将会切过云团的 y=-42 平面替换为 y=-52 附近的浅起伏云底，固定 48×48 网格，随行进更新单个 uniform；使用与云团共享的配色、雾距，避免固定灰底与其他色调脱节。
- 保持 71 draw calls、6 geometries、0 textures；云底从 2 增至 4608 triangles，额外 4606 triangles，没有增加独立渲染循环或纹理。
- 类型检查、lint、36 项测试通过。重新加载模块后查看 1280×720 seed 8472，控制台无错误／警告，截图 cloud-bed-side.png。底色衔接有所缓和，但椭球交界仍明显，单张样本不代表完整视觉验收；需更新其余矩阵与动态验证。

## resize 与故障暂停边界修复

- resize 不再清除 render-error；只在新实例首次成功渲染后解除故障暂停。手动暂停与系统／图形暂停重叠时，resize 不触发额外绘制。
- 新增 session、sleep、graphics、render-error 四种原因下的暂停 resize 回归，37 项测试、类型检查、lint 通过。网页生产构建通过；Windows 最新安装包重新构建中。
- 场景创建中途异常的资源清理仍需单独检查，本次暂停修复不代表该路径已验证。

## 最新 release 平台回归与长测启动

- buildId cf4188d8ce43bc51 的 Windows NSIS 打包完成；SHA-256：E2601D67B99DA37F2BFAF789C73847DE369DC3DCCF81F5EA9B0DA03821D2C0B8。路径 .local/release-build/release/bundle/nsis/LumaWindow_0.1.0_x64-setup.exe。
- 同版本原生平台探针于 11:52:09 UTC 记录 platform-complete，包含上下文恢复后 6 geometries、0 textures、71 draw calls。原始日志已同步；探针进程已停止。未执行安装／卸载或真实拔线操作。
- 同版本 --self-test 长测进程 PID 90448 已启动；独立进程树采样会话 64966，输出 .local/process-90448.jsonl。已观察到第 42 次切换记录，尚未完成两小时。
- 旧版长测与新长测存在并行运行区间，因此不能把这段数据用于单程序干净功耗对照；分别按进程树和 buildId 分析资源与功能证据。

## 2026-09-17 用户视觉反馈与修订（未验收）

用户明确否定旧原型的美观程度、诗意、沿途新鲜感与参数作用幅度。此前静态矩阵与技术测试只作为技术证据，不能视为视觉接受。

本轮对照参考 02-layered-sky / 04-cloud-depth 后：
- 时段从单一灯光强度改成夜色、初光、晨光、日光四套天空／云亮部／云阴影／远景颜色插值，并同步场景灯光。已有夜色、日光端点实际截图 revision-night.png / revision-day.png。
- 云量范围从 170–240 组扩大到 24–240 组，同时控制天空云带；雾距扩大到开阔与浓雾两端。
- 增加种子驱动的连续沿途分区，48 距离单位一段，平滑改变冷暖与云带；不整段重复一张景色。默认行进速度由每秒 0.8 提至 1.8 世界单位，是否适合长期余光观看仍需验证。
- 横版降低轨道在画面中的占比，给天空留出空间；共享云几何做固定轮廓扰动，没有增加几何数量。新天空增加每像素噪声采样，尚不能宣称性能成本不变。
- 类型检查、lint、39 项测试和网页构建通过；新增测试覆盖分区边界连续性与时段整套配色变化，不验证审美。

仍不合格的部分：天空云带过于规则、色阶有条纹感；近景与天空的造型语言未统一，参数尚缺少形态／构图类控制；纵版本轮未回归，新增 GPU 成本未实测。需要继续视觉开发，不能直接以旧版本长测或安装包宣称新版验收。

## 2026-09-17 形态与取景参数

- Params 新增 cloudShape（默认 0.45）与 framing（默认 0.4），均为 0–1 并参加运行时校验、平滑过渡及控制窗口共享。旧参数对象缺字段时由默认值补齐。
- 云层形态同时改变近景云团的宽高比例、天空云带厚度与弯曲幅度；取景远近改变相机 FOV。resize 也采用同一取景倍率，暂停缩放时不应重置构图。
- 天空去掉硬分层量化，并用低频曲线打破平行云带。实际查看 flat-wide 和 tall-close 两端，画面变化明显；高端云团曾呈岩峰感，因此随后收敛高度和几何扰动。两张截图记录的是收敛前调试状态，不作为最终图。
- 39 项测试、类型检查、lint、网页构建通过；参数非法值测试覆盖新增字段。最终云形、竖屏端点及 GPU 开销仍待验证。

## 2026-09-17 竖屏最大参数检查

- 实际在 540×960 将云量、云层形态、取景远近全部设为 1。纵版列车三节完整，已保存 portrait-max-cloud-close.png。
- 同样参数手选横版时发现列车被左边界裁切；调整窄屏横版相机瞄准点，对准列车中段的世界坐标，保持远近参数原范围。
- 修复后重新加载并复现相同组合，三节列车完整且居中，见 portrait-side-close-fixed.png；浏览器错误／警告日志为空。类型检查与 lint 通过。
- 本次是单一种子、极端组合的定位回归，不替代完整多种子矩阵。远景边缘仍有几何片状感、天空和近景语言未完全统一，视觉验收仍未通过。

## 2026-09-17 天空连续云场

- 移除固定高度四条云带，改为四层连续二维噪声覆盖场；云量决定覆盖阈值，形态决定纵横比例，沿途分区控制疏密偏移。用像素导数平滑轮廓，避免硬阈值产生锯齿。
- 靠近地平线淡入雾色。真实网页 1280×720 默认参数已查看并保存 continuous-cloud-sky.png，控制台无错误／警告；固定平行条带已不再出现。
- TypeScript、lint 与生产构建通过。此次为 Shader 修改，没有将单元测试当作 GPU 验证；仍需动态闪烁、性能与跨 WebView 检查。
- 当前仍存在近景云团重叠、远景片状边界、整体粉紫偏同色的问题，不能宣称已达到用户要求的诗意与丰富度。

## 2026-09-17 远景与晨光冷暖调整

- 初光色板改为偏蓝天空、暖奶油云亮部、淡紫阴影，避免整幅粉紫；实际查看 morning-depth-revision.png。
- 山体网格底部从 -65 下移至 -150，基线降至 -80，减少云层外露的矩形边缘；峰部保持可见。
- 云底远处完全收敛到雾色，天空地平线采用同一底色。截图仍见横向色带，随后进一步将天空云层的渐淡区域提高到 UV y=0.52–0.72；这最后的渐淡调整尚需图像复核，不能称边界问题已解决。
- 39 项测试、类型检查与 lint 通过（最后一处仅修改 Shader 常量）。整体美术和动态验收仍未完成。

## 2026-09-17 相机驱动地平线

- 天空新增 horizon uniform，由当前相机方向、投影与远处云底高度计算，替代固定屏幕高度渐淡。每帧相机变化及暂停 resize 都更新；复用 Vector3，不增加对象数或渲染通道。
- 实际检查 1280×720 横版和 540×960 自动纵版，原有硬边变为连续过渡，控制台无错误／警告；雾区偏灰且占比过大，空间层次仍不满意，不能视为视觉验收通过。
- 类型检查、lint、39 项测试通过。此逻辑没有替代当前版本的动态矩阵、长测、GPU 测量或 Mac 实机验证。

## 2026-09-17 连续曲面云团

- 以 Three.js 自带 MarchingCubes 在场景创建时合成云团，五个场源形成连续表面；提取有效 position/normal 后释放临时场网格，播放阶段不重新生成曲面。
- 每组由五个球体实例改为一个云团实例，共享一个几何与材质。单组 760 triangles（旧五球约 2200），实例容量从 1200 降至 240。新增测试检查有限坐标、单位法线、紧凑缓冲区与三角形预算。
- 40 项测试、类型检查、lint、网页生产构建通过。浏览器 seed 8472 样本：71 draw calls、6 geometries、0 textures、约 120–122k triangles；CPU 提交约 0.5ms，仅为瞬时示例。新旧截图时间／视野不同，不作为严格 GPU 或功耗对照。
- 实际截图 merged-cloud-surface.png，控制台无错误／警告。单组内部不再有五球穿插，组与组之间仍可重叠；当前远雾区域过宽、云层偏平、天空造型仍需艺术调整，视觉未验收。
