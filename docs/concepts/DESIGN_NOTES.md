# 第一阶段构图与界面基线

2026-09-15，使用内置 imagegen 生成横／竖概念图，保存在本目录。概念图不是运行时素材，也尚未获得用户的最终美术确认。

- `landscape.png`：完整播放器与右侧设置面板，作为 UI 排列、字号层次和配色参考。
- `portrait.png`：独立纵向斜俯视场景，作为列车位置与弯轨方向参考。

生成提示要点：LumaWindow、安静的程序化云海铁路、灰青天空、米白云和淡紫阴影、奶油／深青短列车、暖窗、细长拱桥；无粒子、无 bloom、无镜头耀斑；横向完整功能界面、纵向无文字构图。

横版生成图自行增加了建筑、树木和精细山体，违背既定范围，实施中明确去掉。场景采用原创几何与简单着色，不使用整张图模拟动态。视觉目标是受控的大形状与空间关系，照片式纹理不作为验收要求。

界面：暖白 `#faf8f4` 设置面板，主文字 `#35484e`，选中态 `#425f65`；系统无衬线字体，标题 18–21px、控件 12–13px，24px 面板内边距，7–9px 控件圆角。桌面面板宽 300px，窄屏使用覆盖面板，场景保持原比例。背景不加额外颜色蒙版。

允许的界面文案来自第一阶段计划：LumaWindow、云海列车、场景设置、构图、云量、雾感、行进速度、光照时段、色调、画质、氛围预览、换一个景色、恢复默认、减少动态效果、显示诊断、测试声音。没有营销说明、统计卡片或装饰标签。

图标使用 lucide 的细线风格。休息文字为原生 HTML，参数为原生表单控件，画面为 WebGL，不能以概念截图替代交互。

视觉差异跟踪：

| 检查点 | 已见问题／决定 | 状态 |
| --- | --- | --- |
| 竖屏主体位置 | 第一轮列车右下裁切，调整相机和列车位置 | 浏览器确认修复 |
| 云体造型 | 单个椭球过于明显，改为有上限的重叠云团 | 继续调校，不宣称最终定稿 |
| 桥梁 | 简单柱桥改为原创拱形高架 | 已实现 |
| 复杂山体与建筑 | 概念图额外生成，不属于已定范围 | 有意省略 |
| 面板与字体 | 暖白、窄面板、低对比控件 | 浏览器已检查 |
| 动态与色调 | 无粒子／光晕，三套受控色板 | 已实现，长时验收进行中 |

最终仍需对最新截图和概念图一起复核，动态美术不能只靠静帧判断。

19:12 调校：修正椭球缩放后的法线，增加云体高度与云隙；雾感控制同时影响云与桥。新增一条低对比静态远山轮廓（240 个三角形），没有增加纹理、后处理或粒子。远山仍偏接近云色，云团的几何拼接感、列车细节及留白仍需审美评估，不据此标记美术定稿。

最新浏览器截图：`../validation/landscape-runtime.png`、`../validation/portrait-runtime.png`、`../validation/ultrawide-runtime.png`。已检查默认种子的横、竖、超宽和方形视口，列车未裁切，基础比例正常。完整多种子动态矩阵尚未完成。
